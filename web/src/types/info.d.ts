export type AllInfo = {
  username: string;
  is_admin: boolean;
  email: string;
  create_at: string;
  update_at: string;
  token: string;
};

export type StoreInfo = {
  username: string;
  email: string;
  is_admin: boolean;
  token: string;
};
